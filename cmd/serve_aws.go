// +build aws_lambda

package cmd

import (
)

func Start(api *operations, port int) error{
	// adapter:= httpadapter.New(api.Serve(nil))
	// logrus.Debug("Starting Lambda....")
	// lambda.Start(adapter.Proxy)
	return nil
}